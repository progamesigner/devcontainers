{
    "id": "herdr",
    "version": "1.2.0",
    "name": "Herdr",
    "description": "Install Herdr and bridge its pane socket(s) in from the Docker host over SSH",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "none"
            ],
            "default": "latest",
            "description": "Select or enter a Herdr version to install"
        },
        "bridge": {
            "type": "boolean",
            "default": true,
            "description": "Bridge the host's HERDR_SOCKET_PATH Unix socket into the container over SSH, so hooks / plugins that report agent state can reach it. Needed on Docker Desktop / OrbStack-style setups, where the host's Unix socket file isn't reachable through a bind mount. Requires Remote Login (sshd) enabled on the host and remoteUser set."
        },
        "remoteUser": {
            "type": "string",
            "default": "",
            "description": "The username to SSH into for the socket bridge. Leave empty to disable the bridge even if `bridge` is true."
        },
        "remoteHost": {
            "type": "string",
            "default": "host.docker.internal",
            "description": "Hostname of the Docker host, reachable from inside the container"
        },
        "remoteSocketPath": {
            "type": "string",
            "default": "",
            "description": "Absolute path of the default session's herdr Unix socket on the Docker host. Leave empty to default to /Users/<remoteUser>/.config/herdr/herdr.sock. Named-session sockets (HERDR_SESSION) are derived from this path's directory as sessions/<name>/herdr.sock next to it — if you point this at a non-standard layout, named sessions won't resolve correctly."
        },
        "privateKeyPath": {
            "type": "string",
            "default": "/var/run/secrets/herdr-devcontainer-bridge/key",
            "description": "Path inside the container where a dedicated bridge SSH private key would be bind-mounted, IF you use one. Optional: if no file exists at this path when the container starts, ssh runs without -i and falls back to whatever auth is otherwise available (e.g. an agent forwarded via SSH_AUTH_SOCK). Re-checked on every reconnect attempt, not just once at container start."
        }
    },
    "entrypoint": "/usr/local/share/herdr-init.sh"
}
